meta {
  name: my
  type: http
  seq: 1
}

get {
  url: {{base-url}}{{prefix-api}}/user
  body: none
  auth: inherit
}

headers {
  : 
}

script:post-response {
  bru.setEnvVar("token", res.body.access_token);
}

settings {
  encodeUrl: true
}
