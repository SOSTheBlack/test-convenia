meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{base-url}}{{prefix-api}}/auth/login
  body: json
  auth: inherit
}

headers {
  : 
}

body:json {
  {
    "email": "admin@example.com",
    "password": "password"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.access_token);
}

settings {
  encodeUrl: true
}
